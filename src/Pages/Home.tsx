export interface HomeProps {}

const Home: React.FC<HomeProps> = () => {
  return (
    <div>
      <h1>Yey, You`re in</h1>
      <button>Create a character</button>
    </div>
  );
};

export default Home;
